# ai-form-filler
This extension allows users to fill out HTML forms on the current tab page using AI. It features a sidepanel window that displays replicated forms. The AI model suggests or automatically fills in the form fields, which the user can then accept or modify. The replicated forms are automatically updated when the user navigates to different tabs.
